

#There are 2 type of blocks
# - resource block = creates and manage the resources on Cloud Environment
# - data source block = fetch the data from existing resources on Cloud Environment

# Each blocks has 2 labels
# first lable = "aws_instance" what kind of resource (Hey I want to work with this resource), pedefined by Terraform
# second lable = "first_ec2" logical name or ID to your block, define by Author

# Argument = Key / Value pairs
# Key = predefine by Terraform
# Value = define by Author

# What is Working Diractory?
# 1. Where are your Terraform configuration files are located
# 2. Where you run the Terraform commands

########################  TERRAFORM BASIC COMMANDS  #########################
#### Terraform init = initialize 
# 1. You can run it once or 1000 times, it does not hurt 
# 2. Installs the plugins, and initialize the backend

#### Terraform plan = Execute plan ######
# Terraform has a Pluge in based Architecture

#### Terraform 


# do not become a root user in the machine when you use CLI #